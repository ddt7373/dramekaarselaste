---
description: Build the app and deploy it to the Axxess FTP server.
---

1. Build the application to generate the latest production files.
// turbo
2. Run `npm run build`

3. Upload the `dist` folder to the FTP server.
// turbo
4. Run `node scripts/deploy.js`

5. Verify that the website is live at https://www.dramekaarselaste.co.za
